import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationView {
            List {
                // Settings options will go here
            }
            .navigationTitle("Settings")
        }
    }
} 